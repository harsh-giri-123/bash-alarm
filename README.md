# Alarm Script

A simple bash script to set alarms with customizable sounds and repeat options.

## Example

![Alarm Example](example.png)

## Usage

Run `./alarmv2.sh` and follow the prompts.

## Requirements

- mpv (install with `sudo apt install mpv`)

## Sounds

Sounds are in the `alarm_sounds/` directory.

Made by harsh giri.